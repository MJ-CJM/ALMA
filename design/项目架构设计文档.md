# ALMA AI 助手 - 项目架构设计文档

## 📋 项目概述

ALMA AI 助手是一个基于 Streamlit 的 Web 应用，集成了 Dify AI 工作流，提供用户聊天、文件上传、管理员后台等功能。项目采用前后端分离的架构设计，使用 SQLModel ORM 进行数据持久化。

## 🏗️ 整体架构

```mermaid
graph TB
    subgraph "前端层"
        A[统一前端 - front.py]
    end
    
    subgraph "业务逻辑层"
        C[认证模块 - auth.py]
        D[后端服务 - backend.py]
        E[数据模型 - models.py]
    end
    
    subgraph "数据层"
        F[MySQL 数据库]
        G[文件存储 - uploads/]
    end
    
    subgraph "外部服务"
        H[Dify AI 工作流]
    end
    
    A --> C
    A --> D
    A --> E
    C --> E
    D --> H
    E --> F
    A --> G
    
    style A fill:#e1f5ff
    style C fill:#fff4e1
    style D fill:#ffe1f5
    style E fill:#e1ffe1
```

## 📁 项目结构

```
ALMA/
├── design/                          # 设计文档目录
│   ├── 项目架构设计文档.md
│   ├── 技术实现细节.md
│   ├── 快速上手指南.md
│   └── README.md
├── web/                            # 核心代码目录
│   ├── front.py                    # 统一前端界面（用户+管理员）
│   ├── auth.py                     # 认证管理模块
│   ├── backend.py                  # Dify 后端服务
│   ├── models.py                   # 数据模型和数据库管理
│   ├── config.json                 # 配置文件
│   ├── start.py                    # 统一启动脚本
│   ├── init_database_sqlmodel.py   # 数据库初始化
│   ├── init_workflows.py          # 工作流初始化（从配置导入）
│   ├── migrate_add_lastlogin.py    # 数据库迁移脚本
│   └── requirements.txt            # 依赖包列表
└── README.md                       # 项目说明
```

## 🔧 核心模块详解

### 1. 数据模型层 (`models.py`)

**设计理念**：使用 SQLModel 实现最简化的 ORM，避免复杂的数据库操作。

#### 核心数据模型

```python
class User(SQLModel, table=True, extend_existing=True):
    """用户表"""
    id: Optional[int] = Field(default=None, primary_key=True)
    username: str = Field(unique=True, index=True, max_length=50)
    password_hash: str = Field(max_length=255)  # 存储明文密码
    invite_code: str = Field(max_length=20)
    created_at: datetime = Field(default_factory=datetime.now)
    last_login: Optional[datetime] = Field(default=None)  # 最后登录时间

class Workflow(SQLModel, table=True, extend_existing=True):
    """工作流配置表"""
    id: Optional[int] = Field(default=None, primary_key=True)
    workflow_id: str = Field(unique=True, index=True, max_length=50)
    name: str = Field(max_length=100)
    description: str = Field(default="", max_length=500)
    api_base: str = Field(max_length=500)
    api_key: str = Field(max_length=500)
    status: str = Field(default="active", max_length=20)  # active, inactive
    is_global: bool = Field(default=True)  # 是否全局可见
    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)

class WorkflowUserPermission(SQLModel, table=True, extend_existing=True):
    """工作流用户权限表（白名单）"""
    id: Optional[int] = Field(default=None, primary_key=True)
    workflow_id: int = Field(foreign_key="workflow.id")
    user_id: int = Field(foreign_key="user.id")
    created_at: datetime = Field(default_factory=datetime.now)

class Conversation(SQLModel, table=True, extend_existing=True):
    """对话表"""
    id: Optional[int] = Field(default=None, primary_key=True)
    user_id: int = Field(foreign_key="user.id")
    conversation_name: str = Field(max_length=100)
    workflow_id: str = Field(max_length=50)
    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)

class Message(SQLModel, table=True, extend_existing=True):
    """消息表"""
    id: Optional[int] = Field(default=None, primary_key=True)
    conversation_id: int = Field(foreign_key="conversation.id")
    role: str = Field(max_length=20)  # user, assistant
    content: str = Field(sa_column=Column(Text))  # 使用 TEXT 类型支持长消息
    created_at: datetime = Field(default_factory=datetime.now)
```

#### 数据库管理类

```python
class DatabaseManager:
    """基于 SQLModel 的数据库管理类"""
    
    def __init__(self, config_file: str = "config.json"):
        self.config = self.load_config()
        self.engine = self._init_engine()
    
    def create_user(self, username: str, password: str, invite_code: str) -> bool:
        """创建新用户 - 支持邀请码验证"""
        
    def save_conversation(self, user_id: int, conversation_name: str, workflow_id: str) -> Optional[int]:
        """保存对话记录"""
        
    def save_message(self, conversation_id: int, role: str, content: str) -> bool:
        """保存消息记录"""
```

**关键设计决策**：
- 使用 `extend_existing=True` 解决 Streamlit 重载导致的表重复定义问题
- 密码采用明文存储，简化认证逻辑
- 支持对话历史持久化和恢复

### 2. 认证模块 (`auth.py`)

**设计理念**：基于邀请码的注册 + 用户名密码登录模式。

#### 认证流程

```python
class AuthManager:
    """认证管理类 - 支持注册和登录"""
    
    def register(self, username: str, password: str, invite_code: str) -> tuple[bool, str]:
        """用户注册流程"""
        # 1. 验证邀请码有效性
        # 2. 检查用户名唯一性
        # 3. 创建用户记录
        # 4. 使用邀请码（增加使用次数）
    
    def login(self, username: str, password: str) -> bool:
        """用户登录流程"""
        # 1. 查询用户信息
        # 2. 验证密码（明文比较）
        # 3. 设置会话状态
```

#### 会话管理

```python
def require_auth(auth_manager: AuthManager) -> bool:
    """检查登录状态，未登录则显示登录页面"""
    if auth_manager.is_logged_in():
        return True
    else:
        auth_manager.show_login_page()
        return False
```

**关键设计决策**：
- 使用 Streamlit 的 `st.session_state` 管理用户会话
- 支持注册和登录的标签页切换
- 邀请码机制控制用户注册

### 3. 后端服务 (`backend.py`)

**设计理念**：封装 Dify API 调用，支持多工作流切换。

#### Dify 集成

```python
class DifyBackend:
    """Dify 后端连接管理类"""
    
    def run_dify_workflow_blocking(self, question: str, user_id: str, workflow_id: str) -> Dict:
        """调用 Dify 工作流（blocking 模式）"""
        url = f"{workflow_config['api_base']}/v1/workflows/run"
        headers = {
            "Authorization": workflow_config['api_key'],
            "Content-Type": "application/json",
        }
        payload = {
            "inputs": {"question": question},
            "response_mode": "blocking",
            "user": user_id,
        }
    
    def parse_dify_response(self, data: Dict) -> str:
        """解析 Dify 响应，兼容不同响应结构"""
        # 支持多种响应格式的解析
```

**关键设计决策**：
- 支持多个 Dify 工作流配置
- 兼容不同的 API 响应格式
- 提供工作流连接测试功能

### 4. 统一前端 (`front.py`)

**设计理念**：统一的前端应用，根据登录类型（用户/管理员）动态渲染不同的界面。

#### 架构设计

```python
# 统一认证检查
is_authenticated, is_admin = require_unified_auth(auth_manager)

# 根据登录类型显示不同界面
if is_admin:
    # 渲染管理员界面
    render_admin_interface()
else:
    # 渲染用户界面
    render_user_interface()
```

#### 用户界面核心功能

```python
# 1. 对话管理（延迟加载优化）
def load_user_conversations():
    """加载用户对话历史（仅加载元数据，延迟加载消息）"""
    
def load_conversation_messages(conv_name: str):
    """按需加载对话消息"""
    
def create_new_conversation(name: str, workflow_id: str):
    """创建新对话（支持唯一名称生成）"""

# 2. 聊天界面
def render_chat_interface():
    """渲染聊天界面（支持消息延迟加载）"""
    
def handle_user_input():
    """处理用户输入和 AI 回复"""

# 3. 文件上传（前端去重）
def handle_file_upload():
    """处理文件上传（会话级去重）"""
```

#### 管理员界面核心功能

```python
# 1. 系统概览
def show_system_overview():
    """显示系统统计信息（用户数、对话数、待审批文件数）"""

# 2. 文件审批
def show_file_approval():
    """文件审批界面（支持批准/拒绝）"""

# 3. 用户管理
def show_user_management():
    """用户管理界面（查看详情、重置密码、删除用户）"""

# 4. 邀请码管理
def show_invite_code_management():
    """邀请码管理（创建、查看、启用/禁用）"""

# 5. 工作流管理
def show_workflow_management():
    """工作流管理（CRUD、权限分配、状态监控）"""
```

**关键设计决策**：
- **统一前端**：用户和管理员在同一应用中，通过登录类型区分
- **性能优化**：使用 `@st.cache_resource` 缓存实例，延迟加载消息
- **工作流数据库化**：工作流配置从数据库读取，支持动态管理
- **权限控制**：工作流支持全局可见和指定用户可见

## 🔄 数据流程

### 用户注册流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as 前端
    participant A as 认证模块
    participant D as 数据库
    
    U->>F: 输入注册信息
    F->>A: 调用 register()
    A->>D: 验证邀请码
    A->>D: 检查用户名唯一性
    A->>D: 创建用户记录
    A->>D: 使用邀请码
    A->>F: 返回注册结果
    F->>U: 显示注册成功
```

### 聊天对话流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as 前端
    participant B as 后端服务
    participant D as Dify API
    participant DB as 数据库
    
    U->>F: 输入消息
    F->>B: 调用工作流
    B->>D: 发送请求
    D->>B: 返回 AI 回复
    B->>F: 解析回复
    F->>DB: 保存消息记录
    F->>U: 显示 AI 回复
```

## ⚙️ 配置管理

### 数据库配置

```json
{
  "database": {
    "host": "101.34.214.7",
    "port": 3306,
    "database": "alma_db",
    "user": "root",
    "password": "wenhantop123",
    "connect_timeout": 30,
    "autocommit": true
  }
}
```

### Dify 工作流配置

**注意**：工作流配置现在支持两种方式：

1. **配置文件方式**（向后兼容）：
```json
{
  "workflows": [
    {
      "id": "workflow_1",
      "name": "通用助手",
      "description": "适用于一般性问答和对话",
      "api_key": "Bearer app-F4F2QM3rjs8DZgTtpIitoxlx",
      "api_base": "http://ai.wenhan.top:8080"
    }
  ]
}
```

2. **数据库方式**（推荐）：
   - 工作流配置存储在 `workflow` 表中
   - 支持通过管理员界面动态管理
   - 支持权限控制（全局可见/指定用户可见）
   - 首次启动时会自动从 `config.json` 导入（如果数据库为空）

## 🚀 启动方式

### 1. 命令行启动（推荐）

```bash
# 启动统一应用（默认端口 8501）
python start.py

# 指定端口启动
python start.py -p 8502

# 初始化数据库
python start.py -i

# 查看帮助
python start.py -h
```

### 2. 数据库初始化

```bash
# 初始化数据库表结构
python init_database_sqlmodel.py

# 从 config.json 导入工作流配置到数据库
python init_workflows.py
```

### 3. 数据库迁移

```bash
# 添加 last_login 字段（如果数据库已有用户表）
python migrate_add_lastlogin.py
```

## 🔧 技术栈

- **前端框架**: Streamlit
- **数据库**: MySQL + SQLModel ORM
- **AI 服务**: Dify API
- **认证**: 邀请码 + 用户名密码
- **文件存储**: 本地文件系统
- **部署**: 支持多端口并发运行

## 📊 数据库设计

### 表关系图

```mermaid
erDiagram
    User ||--o{ Conversation : has
    User ||--o{ UploadedFile : uploads
    Conversation ||--o{ Message : contains
    InviteCode ||--o{ User : used_by
    Workflow ||--o{ WorkflowUserPermission : has
    User ||--o{ WorkflowUserPermission : can_access
    
    User {
        int id PK
        string username UK
        string password_hash
        string invite_code
        datetime created_at
        datetime last_login
    }
    
    Workflow {
        int id PK
        string workflow_id UK
        string name
        string description
        string api_base
        string api_key
        string status
        boolean is_global
        datetime created_at
        datetime updated_at
    }
    
    WorkflowUserPermission {
        int id PK
        int workflow_id FK
        int user_id FK
        datetime created_at
    }
    
    Conversation {
        int id PK
        int user_id FK
        string conversation_name
        string workflow_id
        datetime created_at
        datetime updated_at
    }
    
    Message {
        int id PK
        int conversation_id FK
        string role
        text content
        datetime created_at
    }
    
    UploadedFile {
        int id PK
        int user_id FK
        string filename
        string filepath
        string status
        datetime upload_at
        datetime reviewed_at
    }
    
    InviteCode {
        int id PK
        string code UK
        int max_uses
        int used_count
        string status
        datetime expires_at
        string description
        datetime created_at
    }
```

## 🛡️ 安全设计

### 1. 认证安全
- 邀请码机制控制用户注册
- 用户名唯一性验证
- 会话状态管理

### 2. 数据安全
- 数据库连接池管理
- SQL 注入防护（SQLModel ORM）
- 文件上传路径验证

### 3. 访问控制
- 管理员密码保护
- 用户会话隔离
- 文件访问权限控制

## 🔍 关键代码分析

### 1. 数据库连接管理

```python
def _init_engine(self):
    """初始化数据库引擎"""
    database_url = (
        f"mysql+pymysql://{db_config['user']}:{db_config['password']}"
        f"@{db_config['host']}:{db_config['port']}/{db_config['database']}"
        f"?charset=utf8mb4"
    )
    
    self.engine = create_engine(
        database_url,
        echo=False,
        pool_pre_ping=True,  # 连接池预检查
        pool_recycle=3600,  # 连接回收时间
    )
```

**设计要点**：
- 使用连接池提高性能
- 自动重连机制
- 字符集统一为 utf8mb4

### 2. 会话状态管理

```python
def is_logged_in(self) -> bool:
    """检查用户是否已登录"""
    return "user_id" in st.session_state and "username" in st.session_state

def logout(self):
    """用户登出"""
    keys_to_remove = [
        "user_id", "username", "login_time", "conversations", 
        "current_conversation", "conversation_counter", "rename_mode",
        "user_avatar", "user_name"
    ]
    
    for key in keys_to_remove:
        if key in st.session_state:
            del st.session_state[key]
```

**设计要点**：
- 完整的会话状态清理
- 支持用户信息持久化
- 安全的登出机制

### 3. 工作流管理系统

```python
# 从数据库获取工作流列表（支持权限过滤）
def get_workflow_list(user_id: Optional[int] = None) -> List[Dict]:
    """获取工作流列表"""
    if user_id:
        # 用户：只返回可见的工作流
        return db_manager.get_user_accessible_workflows(user_id)
    else:
        # 管理员：返回所有工作流
        return db_manager.get_all_workflows()

# 工作流权限管理
def set_workflow_users(workflow_id: int, user_ids: List[int]) -> bool:
    """设置工作流的可见用户（白名单）"""
    return db_manager.set_workflow_users(workflow_id, user_ids)
```

**设计要点**：
- **数据库驱动**：工作流配置存储在数据库中，支持动态管理
- **权限控制**：支持全局可见和指定用户可见两种模式
- **向后兼容**：如果数据库为空，自动从 `config.json` 导入
- **API 格式处理**：自动处理 API Key 的 Bearer 前缀和 API Base 的尾部斜杠

## 🚀 部署和运维

### 1. 环境要求
- Python 3.8+
- MySQL 5.7+
- 网络访问 Dify API

### 2. 安装步骤
```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置数据库
# 修改 config.json 中的数据库配置

# 3. 初始化数据库
python init_database_sqlmodel.py

# 4. 启动应用
python start.py -u -m
```

### 3. 监控和维护
- 数据库连接状态监控
- 工作流连接测试
- 文件存储空间管理
- 用户会话管理

## 📈 扩展性设计

### 1. 水平扩展
- 支持多实例部署
- 数据库连接池优化
- 文件存储分离

### 2. 功能扩展
- 插件化工作流
- 自定义认证方式
- 多语言支持

### 3. 性能优化
- 缓存机制
- 异步处理
- 数据库索引优化

## 🎯 开发指南

### 1. 新功能开发
1. 在 `models.py` 中定义数据模型
2. 在 `auth.py` 中添加认证逻辑
3. 在 `backend.py` 中添加服务接口
4. 在前端界面中集成功能

### 2. 调试技巧
- 使用 `echo=True` 查看 SQL 语句
- 利用 Streamlit 的 `st.write()` 调试
- 检查数据库连接状态

### 3. 测试建议
- 单元测试覆盖核心逻辑
- 集成测试验证完整流程
- 性能测试确保并发能力

---

## 📝 总结

ALMA AI 助手采用模块化、可扩展的架构设计，通过 SQLModel ORM 简化数据库操作，使用 Streamlit 提供直观的用户界面，集成 Dify AI 工作流实现智能对话功能。项目支持多用户、多工作流、文件管理等完整功能，适合快速部署和二次开发。

**核心优势**：
- 🏗️ **统一架构**：用户和管理员界面统一在一个应用中
- 🔧 **简化实现**：使用 SQLModel 避免复杂的 SQL 操作
- 🚀 **快速部署**：支持一键启动和数据库初始化
- 🔒 **安全可靠**：邀请码机制和会话管理
- 📈 **易于扩展**：数据库驱动的工作流管理，支持权限控制
- ⚡ **性能优化**：实例缓存和消息延迟加载，提升响应速度
- 👥 **用户管理**：完整的用户管理功能（查看、重置密码、删除）
